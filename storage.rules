rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite leitura e escrita se o usuário estiver autenticado (comum para dev)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}